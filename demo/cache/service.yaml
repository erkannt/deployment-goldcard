apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: cache
  name: sciety--demo--cache
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: redis
  selector:
    app.kubernetes.io/component: cache
  type: ClusterIP